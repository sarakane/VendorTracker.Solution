@{
  Layout = "_Layout";
}

@using VendorTracker.Models;

<div class="container">
  <h3>Here are all of the orders for @Model["vendor"].Name:</h3>
  @if(@Model["orders"].Count == 0)
  {
    <p>You don't have any orders for this vendor.</p>
  }
  <ol>
    @foreach (Order order in @Model["orders"])
    {
      <li><a href='/vendors/@Model["vendor"].Id/orders/@order.Id'>@order.OrderTitle</a></li>
    }
  </ol>
  <h4><a href='/vendors/@Model["vendor"].Id/orders/new'>Add a new order</a></h4>
  <h4><a href="/vendors">Return to vendor list</a></h4>
  <h4><a href="/">Return to main page</a></h4>
</div>